@model WalutyBusinessLogic.Models.LocalExtremeValueModel
@{
    ViewData["Title"] = "LocalExtremeOfForm";
    var items = ViewData["currencyCodes"];
}

<h1>Local extreme</h1>

<div class="container">
    <form asp-controller="LocalExtreme" asp-action="ShowLocalExtreme" method="post">
        <div class="form-group">
            <label asp-for="NameCurrency">Currency </label>
            <select asp-for="NameCurrency" asp-items="(IEnumerable<SelectListItem>)@items" class="form-control">
                <option disabled="true" selected="true">Select Currency</option>
            </select>
            <span asp-validation-for="NameCurrency" class="text-danger"></span>
            @if (ViewBag.DateRangeForLocalExtreme != null)
            {
                <span class="text-danger">@ViewBag.DateRangeForLocalExtreme</span>
            }
        </div>
        <div class="form-group">
            <label asp-for="StartDate">Start date</label>
            <input type="date" asp-for="StartDate" name="startDate" class="form-control" />
            <span asp-validation-for="StartDate" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="EndDate">End date</label>
            <input type="date" asp-for="EndDate" name="endDate" class="form-control" />
            <span asp-validation-for="EndDate" class="text-danger"></span>
        </div>
        <button type="submit" class="btn btn-success">Show</button>
    </form>
</div>
